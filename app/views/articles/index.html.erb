<h1>Articles Listing Page</h1>

<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Actiond</th>
        </tr>
    </thaead>

    <tbody>
        <% @articles.each do |article| %>
            <tr>
                <td><%= article.title %></td>
                <td><%= article.title %></td>
                <td>SomeActions</td>
            </tr>
        <% end %>
    </tbody>
</table>